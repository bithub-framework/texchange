import { MODELS as BASE_MODELS, TYPES as BASE_TYPES } from '../types';
export declare class MODELS extends BASE_MODELS {
    static readonly initialSettlementPrice: unique symbol;
}
export declare class TYPES extends BASE_TYPES {
    static readonly MODELS: typeof MODELS;
}
